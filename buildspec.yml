version: 0.2

phases:
  install:
    commands:
      - npm install
  build:
    commands:
      - npm run test
artifacts:
  type: zip
  files:
    - app.js
    - package.json
    - routes/*
    - views/*
